const $=selector=>document.querySelector(selector);const esc=value=>String(value??"").replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[char]);
const api=async(url,options={})=>{const response=await fetch(url,options);const data=await response.json();if(response.status===401){location.href="/login";throw new Error("登录已过期");}if(!response.ok)throw new Error(data.error||"请求失败");return data;};
let lastCredential="";function showCredential(email,password){lastCredential=`邮箱：${email}\n临时密码：${password}`;$("#credentialText").textContent=lastCredential;$("#credentialBox").hidden=false;}
async function load(){const session=await api("/api/auth/session");if(!session.user||session.user.role!=="admin")return location.href="/";$("#adminIdentity").textContent=`管理员：${session.user.email}`;const [usersData,requestsData]=await Promise.all([api("/api/admin/users"),api("/api/admin/requests")]);$("#userRows").innerHTML=usersData.users.map(user=>`<tr><td>${esc(user.email)}</td><td>${user.role==="admin"?"管理员":"试用用户"}</td><td><span class="pill ${user.active?"":"off"}">${user.active?"已启用":"已停用"}</span></td><td>${user.must_change_password?"待修改":"已完成"}</td><td>${esc(user.last_login_at||"—")}</td><td><span class="row-actions"><button data-reset="${user.id}" data-email="${esc(user.email)}" data-role="${user.role}">重置密码</button><button data-toggle="${user.id}" data-active="${user.active}">${user.active?"停用":"启用"}</button></span></td></tr>`).join("")||'<tr><td class="empty" colspan="6">暂无用户</td></tr>';const pending=requestsData.requests.filter(item=>item.status==="pending");$("#requestRows").innerHTML=pending.map(item=>`<tr><td>${esc(item.email)}</td><td>${esc(item.note||"—")}</td><td>${esc(item.requested_at)}</td><td><span class="row-actions"><button data-approve="${item.id}">批准</button><button data-reject="${item.id}">拒绝</button></span></td></tr>`).join("")||'<tr><td class="empty" colspan="4">暂无待审批申请</td></tr>';bindRows();}
function bindRows(){document.querySelectorAll("[data-toggle]").forEach(button=>button.onclick=async()=>{await api(`/api/admin/users/${button.dataset.toggle}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({active:button.dataset.active!=="true"})});await load();});document.querySelectorAll("[data-reset]").forEach(button=>button.onclick=async()=>{const data=await api("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:button.dataset.email,role:button.dataset.role})});showCredential(data.email,data.temporaryPassword);await load();});document.querySelectorAll("[data-approve]").forEach(button=>button.onclick=async()=>{const data=await api(`/api/admin/requests/${button.dataset.approve}/approve`,{method:"POST"});showCredential(data.email,data.temporaryPassword);await load();});document.querySelectorAll("[data-reject]").forEach(button=>button.onclick=async()=>{await api(`/api/admin/requests/${button.dataset.reject}/reject`,{method:"POST"});await load();});}
$("#addUserForm").onsubmit=async event=>{event.preventDefault();try{const data=await api("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:$("#newUserEmail").value,role:$("#newUserRole").value})});showCredential(data.email,data.temporaryPassword);$("#newUserEmail").value="";await load();}catch(error){$("#adminMessage").textContent=error.message;}};$("#copyCredential").onclick=async()=>{await navigator.clipboard.writeText(lastCredential);$("#adminMessage").textContent="登录凭据已复制";};$("#logoutButton").onclick=async()=>{await api("/api/auth/logout",{method:"POST"});location.href="/login";};load().catch(error=>{$("#adminMessage").textContent=error.message;});
